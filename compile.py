#we say python is interpretor language. But there is inbuilt compiler in python which does the compilation too.
import py_compile
py_compile.compile('app.py')
print('COMPILATION DONE')

#this produces the compilation file in cache.
